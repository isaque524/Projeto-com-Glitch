<!DOCTYPE html>
<html lang="en">
<head>
  
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
       <title>Tabela</title>
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link rel="stylesheet" href="/tabela.css" />
   <script src="https://kit.fontawesome.com/723f0cb31d.js" crossorigin="anonymous"></script>

</head>
<body>
  {{>navbar}}
    <div class="row">
       <header>
    <h1>Meus Jogos</h1>
  </header>  
    <!-- loop inicio -->
    {{#each demos}}
      <div class="column">
        <table class="table">
          
          <thead>
            <tr>
              <th class="nome">nome</th>
              <th class="chave">chave</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td style="  padding-right:158px;width:19%;transform:translatex(15px) translatey(22px);height:130px;margin-bottom: 15px;background-size: cover;border-radius: 15px 15px 0 0 ;background-image: url('{{imagem}}');"></td>
              <td class="nome">{{nome}}</td>
              <td class="chave">{{chave_produto}}</td>
            </tr>
          </tbody>
          
        </table>
      </div>
    {{/each}}
    <!-- fim do loop -->
  </div>

</body>
</html>